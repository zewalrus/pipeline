/* ============================================================
   Your Firebase settings. Edit this file once and leave it alone.

   index.html can be replaced any time without touching this — that's
   the whole point of it being separate.

   Values come from: Firebase console → Project settings →
   Your apps → Web app → SDK setup and configuration → Config.

   These four values are safe to have in a public repo. Firebase web
   keys identify your project, they don't grant access — your Firestore
   security rules are what protect the data. Your Firebase account
   PASSWORD is a different thing entirely and never belongs in a file.
   ============================================================ */

window.FIREBASE_CONFIG = {
  apiKey: "AIzaSyCEitxdKFPDDOLMObRFKVoVM9eFohjBeQ0",
  authDomain: "content-pipeline-36dbf.firebaseapp.com",
  projectId: "content-pipeline-36dbf",
  appId: "1:706373536416:web:f492bfe28d3fce533317c5
"
};
